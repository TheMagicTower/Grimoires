{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "toolName == 'Bash' && toolInput.command matches 'codex|gemini'",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "CLI 호출 완료. 결과를 사용자에게 전달하세요."
          }
        ]
      }
    ]
  }
}
